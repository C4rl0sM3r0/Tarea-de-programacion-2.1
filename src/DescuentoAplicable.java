// Interface que define la capacidad de aplicar descuentos a los productos
public interface DescuentoAplicable {
    // Método que aplica un porcentaje de descuento al precio base
    double aplicarDescuento(double porcentaje);
}
